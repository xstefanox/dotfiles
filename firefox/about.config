# disable automatic check and disabling for old plugins
user_pref("extensions.blocklist.enabled", 1);

# remove the dotted box around clicked buttons
user_pref("browser.display.focus_ring_width", 0);

# disable spellcheck
user_pref("layout.spellcheckDefault", 0);

